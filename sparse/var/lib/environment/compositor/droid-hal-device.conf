# Config for planet/aeon6797_6m_n
EGL_PLATFORM=null
QT_QPA_PLATFORM=surfaceflinger

# Camera doesn't work with hwcomposer yet, this needs to be investigated
# EGL_PLATFORM=hwcomposer
# QT_QPA_PLATFORM=hwcomposer

# Determine which node is your touchscreen by checking /dev/input/event*
LIPSTICK_OPTIONS=-plugin evdevtouch:/dev/input/event8:rotate=270 -plugin evdevkeyboard:keymap=/usr/share/qt5/keymaps/droid.qmap
